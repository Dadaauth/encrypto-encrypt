#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "main.h"
/**
 * main - the main function of our program
 * Return: 0 if program is successful
*/
int main(int ac, char **av)
{

    interactive_mode();
    // int key = 1;
    // char *encfile;

    // if (av[1] == NULL)
    // {
        // interactive_mode();
    //     return (0);
    // }
    // if (av[2] != NULL)
    //     key = atoi(av[2]);

    // encfile = encrypt(av[1], key);
    // if (encfile == NULL)
    //     return (-1);
    // if (decrypt(encfile, key) == NULL)
    //     return (-1);
}
